import { postsRouter as posts } from "./posts.router.js";
import { usersRouter as users } from "./users.router.js";
import { accountsRouter as accounts } from "./accounts.router.js";

export default { posts, users, accounts };
